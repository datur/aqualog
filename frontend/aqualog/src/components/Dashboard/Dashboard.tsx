import React from "react";

export default function Dashboard(){
    
    /*
    TODO:

    - Call Backend API to see if user exists
        - If doesnt exist redirect to signup
        - If exists get tanks for user
            - If user has tanks show Charts for users tanks
            - If user not have tanks show button for adding first tank

        - Navbar now shows Dashboard, Tanks, Add reading


    */

    return (
        <div>
            <h1>Dashboard</h1>
        </div>
    )
}